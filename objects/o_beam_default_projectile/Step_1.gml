/// @description Insert description here
// You can write your code in this editor
if(!setup) {
	setup = true;
	xpf = (endx - tx) / lifespan;
	ypf = (endy - ty) / lifespan;
	total_damage = damage;
	damage = damage / lifespan; // adjust damage to per-frame values
}